# Day 1 – Python Basics

## What I Learned
- How to store values using variables
- Difference between numbers and text (strings)
- Type conversion using `str()`
- Comparing values using operators like `>`
- Using `if` and `else` for decision making

## Simple Example
```python
income = 2000
expenses = 3200

if income > expenses:
    print("This person is saving money")
else:
    print("This person is not saving money")

### Step 8
Rename the file to:

```text
day_01_basics.md